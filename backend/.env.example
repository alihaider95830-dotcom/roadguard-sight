# Flask Configuration
SECRET_KEY=change-this-to-a-random-secret-key
JWT_SECRET_KEY=change-this-to-a-different-secret-key

# Database (default: SQLite in backend/atis.db)
# DATABASE_URL=sqlite:///atis.db
# DATABASE_URL=postgresql://user:password@localhost/atis

# ML Model Path (absolute path to your trained model file)
# ML_MODEL_PATH=/path/to/your/model.h5
